[package]
name = "nessy_bin"
version = "0.1.0"
edition = "2024"

[dependencies]
libnessy = { version = "0.1.0", path = "../libnessy" }
sdl2 = "0.38.0"
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.1"
bitflags = "2.10.0"
rand = "0.10.0"

[dev-dependencies]
tempfile = "3.23.0"
